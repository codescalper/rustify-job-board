/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "xwazqkxjoqz4ccib.public.blob.vercel-storage.com",
      },
    ],
  },
};

export default nextConfig;
